# 开发日志 2026-08-12

## 今日完成

- [修复] 识别完第一批后导入新文件再点开始，会把第一批已成功的文件重新识别一遍。
  - 根因：`_on_start()` 每次把列表所有任务交给 worker，`reset_statuses()` 重置全部状态。
  - 修复：跳过已成功任务（[app/ui/file_list_model.py](../app/ui/file_list_model.py)、[app/ui/worker.py](../app/ui/worker.py)、[app/ui/main_window.py](../app/ui/main_window.py)）。
  - 验证：pytest 18 项通过。

- [重构] 处理管线改为两阶段：**并行提取音频 → 串行 API 识别**。
  - 新增设置项「并行提取数」（1-8，默认 2，[app/ui/settings_dialog.py](../app/ui/settings_dialog.py)）。
  - 提取的 WAV 缓存到系统临时目录 `Scrybe_cache`，识别失败重试可复用、跳过提取（[app/core/pipeline.py](../app/core/pipeline.py)）。
  - worker 用 `ThreadPoolExecutor` 并行提取，再按队列顺序逐个识别，一个返回后才识别下一个（[app/ui/worker.py](../app/ui/worker.py)）。
  - 验证：新增 [tests/test_worker_offline.py](../tests/test_worker_offline.py)（断言提取并行峰值 ≥2、识别并发恒为 1、顺序固定）、[tests/test_pipeline_offline.py](../tests/test_pipeline_offline.py)（缓存复用）。

- [功能] 新增「↻ 重试失败」按钮：把失败项重置为待处理，重试时命中 WAV 缓存直接识别，不再重新提取（[app/ui/main_window.py](../app/ui/main_window.py)）。

- [发布] 打包新版 exe：`dist/Scrybe.exe`（86.4 MB），经 `build.ps1` 全流程（装依赖 → 18 项测试 → PyInstaller），启动冒烟通过。

- [文档] 建立项目文档体系：新增 [CLAUDE.md](../CLAUDE.md)、[docs/](../docs/)（需求/技术方案/设计规范/执行步骤）、[devlog/](../devlog/)。

## 待办事项

- [ ] 清理 dashscope 打包警告（`finetune.reinforcement` 依赖可选 pydantic/yaml），可在 [build.spec](../build.spec) 排除无关子模块。
- [ ] 评估更激进的流水线：提取完一个文件即进入识别（当前是先全部提取完再逐个识别，两阶段间有小尾巴等待）。
- [ ] 验证并行提取在高负载（8 并发）下的 CPU 占用与稳定性。
