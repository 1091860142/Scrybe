# 开发日志 2026-08-11

## 已完成
- 需求沟通与确认（GUI、中文为主、阿里云百炼 + OpenAI 兼容、统一输出目录、逐个排队、单 exe）。
- 制定并批准开发计划；创建项目基础设施（目录、requirements、docs×4、devlog、CLAUDE.md）。
- 技术选型调整：~~ffmpeg.exe 内置~~ → **PyAV**（本机网络无法可靠下载 ffmpeg 二进制；PyAV 同样基于 FFmpeg 解码库，功能等价且 exe 更小）。
- 核心模块全部编写：models / config / srt_builder / wav_util / audio_chunker / audio_util(PyAV) / providers(dashscope, openai_compat) / pipeline / UI(main_window, settings, file_list_model, worker)。
- 依赖安装完成：PySide6 6.11.1、requests、pytest、PyAV 15.1.0、dashscope 1.26.6（均从阿里云 PyPI 镜像安装）。
- 单元测试 16 个全部通过（SRT 生成 / 配置 / WAV 时长 / 管线偏移合并 / OpenAI 兼容解析）。
- 音频链路冒烟通过：3s 转码 3000ms、30s 切 3 块偏移 [0,10000,20000]。
- 主窗口离屏构造成功；ffmpeg 相关模块已清理（用 PyAV 替代）。
- **百炼 API 实测打通**：WebSocket 流式（SDK Recognition，paraformer-realtime-v2）无需 OSS；官方示例语音识别成功：
  `00:00:00,280 --> 00:00:03,820 Hello word, 这里是阿里巴巴语音实验室。`
  （排除了 REST 文件上传 `/api/v1/files`→OSS URL 被转录服务拒绝的方案。）
- 用户百炼 API Key 已保存到 `%APPDATA%\Scrybe\config.json`（不回显）。

- **打包完成**：`dist\Scrybe.exe`（86MB），离屏启动正常（冻结程序各模块加载成功），PyInstaller 警告均为 Windows 正常缺失（POSIX 模块）。
- **编解码路径验证**：合成 MP3 / MP4(H264+音频) → transcode 解码时长正确；Pipeline 端到端（真实配置+真实百炼 Provider+官方示例语音）正确写出 `sample_speech.srt`。
- README 已写；docs/02 已更新为最终百炼方案。

## 待办
- [ ] 用户用真实 mp3/mp4 文件验收 GUI 端到端（百炼路径）。
- [ ] OpenAI 兼容路径的真实端点验证（需用户提供 base_url/key/model；离线 mock 已过）。
- [ ] 若用户需要，完善 README 与首次使用说明。
