# Scrybe —— Windows 视频/音频批量转字幕工具

中文图形界面软件：批量把本地 MP4/MP3 等媒体通过语音识别 API（阿里云百炼 / OpenAI 兼容接口）转成同名 `.srt` 字幕，交付单个 exe。

## 项目标准文件（先读这里）

- `docs/01-需求文档.md` —— 产品需求与验收标准
- `docs/02-技术方案.md` —— 技术选型、架构、模块职责、协议细节
- `docs/03-设计规范.md` —— 代码/文档规范、devlog 模板
- `docs/04-执行步骤.md` —— 实施清单（每步含验证方式）

## 工作指引

1. **改动代码前**：先读 `docs/02-技术方案.md` 了解模块职责与约束，遵守 `docs/03-设计规范.md`。
2. **每天维护 `devlog/YYYY-MM-DD.md`**：记录当天**已完成事项**与**待办事项**（按模块记），格式见 `docs/03-设计规范.md` 模板。每个工作日结束时更新。
3. **关键技术决策**：同步写回 `docs/` 对应文档（如百炼上传方式实测结果、接口细节修正）。
4. **API Key**：存于本机 `%APPDATA%\Scrybe\config.json`；不回显、不入文档、不提交到任何公开仓库。
5. **测试**：离线可测的部分必须写 `tests/` 单测（`pytest`）；真实 API 用 `tools/` 下的 live 脚本实测。
6. **打包**：`build.ps1` 一键生成 `dist\Scrybe.exe`（onefile + windowed，内置 ffmpeg）。
