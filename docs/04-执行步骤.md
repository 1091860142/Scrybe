# 04 执行步骤（实施清单）

> 每步完成即在其后打 `[x]`，并在 devlog 记录；核心步骤附验证方式。

- [x] 1. **基础设施**：目录、requirements.txt、devlog/、docs/（01~04）、CLAUDE.md；`app/main.py` 空窗口跑通。
- [x] 2. **models.py + config.py**：`tests/test_config.py` 通过。
- [x] 3. **srt_builder.py**：`tests/test_srt_builder.py` 通过（精确文本/合并/边界）。
- [x] 4. **wav_util.py + audio_chunker.py**：Python `wave` 生成 WAV 测时长读取通过。
- [x] 5. **音频转码（PyAV 方案，替代 ffmpeg）**：合成 MP3 / MP4(H264+音频) 解码转码时长正确；30s 切 3 块偏移正确。
- [x] 6. **providers/base.py** + `create_provider` 工厂。
- [x] 7. **openai_compat.py**：`tests/test_openai_compat.py` 通过（verbose_json 解析/毫秒换算）。
- [x] 8. **dashscope.py**：改用官方 SDK `Recognition`（WebSocket 流式，无需 OSS），真实 Key 实测通过（官方示例语音）。
- [x] 9. **pipeline.py**：FakeProvider 两块偏移合并 + SRT 写盘通过。
- [x] 10. **UI 骨架**：settings_dialog / file_list_model / main_window（离屏构造成功）。
- [x] 11. **worker.py + 信号接线**：代码完成；FakeProvider 队列验证（进度/失败跳过/停止）待最终 GUI 验收。
- [x] 12. **真实 API 实测**：百炼 WebSocket 路径确认并实测通过；OpenAI 兼容仅离线 mock（真实端点待用户提供）。
- [x] 13. **打磨**：utf-8-sig、拖拽、汇总弹窗、关闭时处理、中文字体。
- [x] 14. **打包 + 冒烟**：`build.ps1`/`build.spec` → `dist\Scrybe.exe`（86MB）离屏启动正常。
- [ ] 15. **用户验收**：用真实 mp3/mp4 在 GUI 中完成一次转换；OpenAI 兼容真实端点验证。
